import styled from 'styled-components';

interface IProps {
  bgImg: string;
  title: string;
  subTitle: string;
  startDate: string;
}

const CampCard = ({ bgImg, title, subTitle, startDate }: IProps) => {
  return <Container></Container>;
};

export default CampCard;

const Container = styled.div``;
